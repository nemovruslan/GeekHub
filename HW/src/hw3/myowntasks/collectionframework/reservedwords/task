Написать код, который принимает через параметр командной строки путь к текстовому

файлу (исходник на java), читает текст программы из файла, разбивает на слова и строит мапу

<String, Integer>. Где ключ - зарезервированное слово языка Java, а значение сколько раз это слово

встречается в файле (остальные слова - не учитываем!).

Выберите подходящую реализацию мапы, которая лучше всего подходит, чтобы выводить а) слова

отсортированные в алфавитном порядке; б) слова отсортированные по количеству повторений в

файле.