Кастомная сортировка списка объектов класса Car, у которого есть поле name и price.

Необходимо написать сортировку Collections.sort(List<Car>, Comparator<Car>); по названию и по

цене (если совпадает 3-я буква в двух объектах, которые сравниваются, то дополнительно

сортируем по цене в Asc). Comparator передать как анонимный класс.